def main(): int {
    var a = 5, b = 10: int;
    var x = true, y = true: bool;

    a = a + b;
    a = a * b;
    a = a / b;
    a = a - b;
    a = a % b;
    
    x = a < b;
    x = a > b;
    x = a <= b;
    x = a >= b;

    x = (x && y) || (a > 5);

    a = -5;
    b = -a;


    return 0;
}