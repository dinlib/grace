def mul(a: int, b: int): int {
  return a * b;
} 

def main():int {
  var a = 10, result: int;

  result = mul(a, b); // Variável b não declarada
  result = b;         // Variável b não declarada


  skip;   // Comando skip fora de laço

  stop;   // Comando stop fora de laço

}
