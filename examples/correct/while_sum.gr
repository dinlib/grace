
def sum(x: int, y: int): int {
	return x + y;
}

def main(): int {
	var x, r = 0, i: int;

	x = 2;

	i = 0;
	while (i < 100) {
		i += 1;
		if (i % 2 == 0) {
			skip;
		}
		write i;	
	}

	write i;

	return 0;
}

