
def sum(x: int, y: int): int {
     return x + y;
}

def main(): int {
    var x = 4, y = 30: int;
    var z: int;

    var b: bool;

    b = x;

    z = sum(x, y);

    write "%d\n" % z;

    return 0;
}
