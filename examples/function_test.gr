
def sum(x: int, y: int): int {
     return x + y;
}

def main(): int {
    var x, y, z, i: int;

    x = 2;
    y = 6;

    for (i = 0; i < 10; i = i + 1) {
        z = sum(sum(x, y), 100 * i);
    }

    write z;

    return 0;
}
