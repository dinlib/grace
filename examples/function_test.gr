
def sum(x: int, y: int): int {
     return x + y;
}

def sumvec(v[]: int, n: int): int {
    return n;
}

def main(): int {
    var v[10] = {1, 2, 3, 4, 5, 6, 7, 8, 9, 0}: int;
    var n = 10: int;

    var z = 2: int;

    z = sumvec(v, n);

    return z;
}
